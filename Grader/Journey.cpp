#include <bits/stdc++.h>

using namespace std;
int n;
vector<vector<int>> h;

int main()
{
    cin >> n;

    h.resize(n, vector<int>(n));
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j, n; j++)
        {
            cin >> h[i][j];
        }
    }
    return 0;
}