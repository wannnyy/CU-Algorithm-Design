#include <bits/stdc++.h>
#define ll long long
using namespace std;
ll n, m, k, w;
vector<ll> p, h;

int main()
{
    cin >> n >> m >> k >> w;
    p.resize(m);
    for (int i = 0; i < m; i++)
    {
        cin >> p[i];
    }
    for (int i = 0; i < m; i++)
    {
        cin >> h[i];
    }
}