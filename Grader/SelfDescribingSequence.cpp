#include <bits/stdc++.h>

#define ll long long

using namespace std;
int n;
vector<ll> v;

int main()
{
    cin >> n;
    v.resize(n);
    for (int i = 0; i < n; i++)
    {
        cin >> v[i];
    }
    return 0;
}