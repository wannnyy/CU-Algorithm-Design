#include <bits/stdc++.h>

using namespace std;
int p, k, A, B;
vector<int> v;

int main()
{
    cin >> p >> k >> A >> B;
    v.resize(k);
    for (int i = 0; i < k; i++)
    {
        cin >> v[i];
    }

    return 0;
}