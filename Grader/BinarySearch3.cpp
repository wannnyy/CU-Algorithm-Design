#include <iostream>
#include <vector>
using namespace std;

// int bis(vector<int> &v, int q)
// {
    
// }

int main()
{
    int n, m;
    cin >> n >> m;
    vector<int> v(n), q(m);
    for (int i = 0; i < n; i++)
    {
        cin >> v[i];
    }
    for (int i = 0; i < m; i++)
    {
        cin >> q[i];
    }




    return 0;
}